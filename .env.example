# 平台 API 配置示例
# Platform API Configuration Example

# 全局配置 (Global Configuration)
# 所有平台的默认运行模式: mock (模拟) 或 real (真实)
PLATFORM_MODE=mock

# 所有平台的默认超时时间 (毫秒)
PLATFORM_TIMEOUT_MS=8000

# 抖音平台配置 (Douyin Platform Configuration)
# 运行模式: mock 或 real
DOUYIN_MODE=mock
# API 端点 URL (仅在 real 模式下需要)
DOUYIN_PUBLISH_URL=https://api.douyin.com/publish
# 应用 ID (仅在 real 模式下需要)
DOUYIN_APP_ID=your-douyin-app-id
# 应用密钥 (仅在 real 模式下需要)
DOUYIN_APP_SECRET=your-douyin-app-secret
# 超时时间 (毫秒，可选)
DOUYIN_TIMEOUT_MS=8000

# 小红书平台配置 (Xiaohongshu Platform Configuration)
XIAOHONGSHU_MODE=mock
XIAOHONGSHU_PUBLISH_URL=https://api.xiaohongshu.com/publish
XIAOHONGSHU_APP_ID=your-xiaohongshu-app-id
XIAOHONGSHU_APP_SECRET=your-xiaohongshu-app-secret
XIAOHONGSHU_TIMEOUT_MS=8000

# 头条平台配置 (Toutiao Platform Configuration)
TOUTIAO_MODE=mock
TOUTIAO_PUBLISH_URL=https://api.toutiao.com/publish
TOUTIAO_APP_ID=your-toutiao-app-id
TOUTIAO_APP_SECRET=your-toutiao-app-secret
TOUTIAO_TIMEOUT_MS=8000

# 配置说明 (Configuration Notes):
# 1. 平台特定的环境变量会覆盖全局配置
# 2. 在 mock 模式下，不需要配置 API 凭证
# 3. 在 real 模式下，必须配置有效的 publishUrl、appId 和 appSecret
# 4. 超时时间单位为毫秒，默认值为 8000 (8秒)
